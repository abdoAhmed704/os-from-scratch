# Unix Utilities

Simple implementations of common Unix commands in C.

## Commands

* `cp` — Copy files
* `mv` — Move/rename files
* `echo` — Print text
* `pwd` — Print the current working directory

Each command has its C source file (`*.c`) and compiled executable.

